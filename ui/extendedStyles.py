class ExtendedStyles(object):

    # Colours Buttons
    def extendButtons(*buttons):
        for button in buttons
            button.setStyleSheet("background-color:#2f2f2f; color:#e1e1e1")

    # Colours Boxes
    def extendBoxes(*boxes):
        for box in boxes:
            box.setStyleSheet("background-color:#2f2f2f; color:#e1e1e1")

    # Colours Widgets
    def extendWidgets(*widgets):
        for widget in widgets:
            widget.setStyleSheet("background-color:#666666")

    # Colours Labels
    def extendLabels(*labels):
        for label in labels:
            label.setStyleSheet("color:#e1e1e1")
